sudo: required
language: java
jdk:
  - oraclejdk8
script:
    - ./gradlew clean build
    - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
    - ./gradlew distDocker
services:
  - docker
